<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key><string>WorktimeBar</string>
    <key>CFBundleDisplayName</key><string>Worktime</string>
    <key>CFBundleIdentifier</key><string>com.oliver.worktime-bar</string>
    <key>CFBundleExecutable</key><string>WorktimeBar</string>
    <key>CFBundlePackageType</key><string>APPL</string>
    <key>CFBundleShortVersionString</key><string>1.0</string>
    <key>CFBundleVersion</key><string>1</string>
    <key>LSMinimumSystemVersion</key><string>13.0</string>
    <!-- Menu-bar-only: no Dock icon, no app switcher entry. -->
    <key>LSUIElement</key><true/>
    <key>NSHighResolutionCapable</key><true/>
    <!-- Required to ask Chrome for the front tab. Without this key macOS does
         not deny the Apple Event after asking -- it never asks: the app is
         refused before any dialog appears, osascript exits non-zero, and
         chromeActiveTab() reads that as "Chrome has no windows open". The
         focus log then records every Chrome row with no tab and no url, so
         the browser earns nothing however work-shaped the page is. Three days
         of focus logs carried zero tab fields for exactly this reason. -->
    <key>NSAppleEventsUsageDescription</key><string>Worktime reads the title and address of the front Chrome tab, so a work page in the foreground counts as work and a personal one does not.</string>
</dict>
</plist>
